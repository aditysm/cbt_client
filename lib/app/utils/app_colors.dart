import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const primaryBlue = Color(0xFF3673FF);
  static const primaryYellow = Color(0xFFFFC107);
  // static const primaryGreen = Color(0xFF4CAF50);
  static const primaryRed = Color(0xFFE53935);

  // Backgrounds
  static const background = Color(0xFFFFFFFF);
  static const backgroundAlt = Color(0xFFF7F9FC);
  static const surface = Color(0xFFF0F4F8);

  // Texts
  static const text = Color(0xFF212121);
  static const textLight = Color(0xFF757575);
  static const textInverse = Color(0xFFFFFFFF);

  // Status colors
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFFFA000);
  static const error = Color(0xFFD32F2F);
  static const info = Color(0xFF0288D1);

  // Border & Divider
  static const border = Color(0xFFE0E0E0);
  static const divider = Color(0xFFBDBDBD);

  // Special focus/hover
  static const focus = Color(0xFF64B5F6);
}
